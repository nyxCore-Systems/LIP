name: lip
version: 0.1.0
description: >
  LIP — Linked Incremental Protocol: Dart bindings.
  First-class support for Dart/Flutter projects (spec §5.2).

homepage: https://github.com/lip-protocol/lip

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
